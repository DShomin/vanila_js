## 설정 방법

1. 이 저장소를 클론합니다.
2. OpenWeatherMap에서 API 키를 발급받습니다.
3. 프로젝트 루트 디렉토리에 `.env` 파일을 생성합니다.
4. `.env` 파일에 다음과 같이 API 키를 입력합니다:
   ```
   API_KEY=여기에_발급받은_API_키를_입력하세요
   ```
5. 웹 서버를 사용하여 `index.html` 파일을 실행합니다.